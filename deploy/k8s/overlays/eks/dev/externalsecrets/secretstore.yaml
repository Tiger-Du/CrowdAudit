apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secretsmanager
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1 # should match Terraform/AWS region
      auth:
        jwt:
          serviceAccountRef:
            name: externalsecrets-sa
